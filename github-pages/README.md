Public homepage + privacy policy for the Google OAuth consent screen.
Upload `index.html` to a **separate public GitHub repo** and enable GitHub Pages.
Do NOT push the scheduler project itself (it contains scheduler.db and client_secret*.json).
